algoritmo "GRAU_RAD"
var
   Angulo, S: REAL

inicio

      Escreval("Informe um Ângulo: ")
      leia (angulo)
      S <- Sen(GraupRad(Angulo))
      Escreva("O seno de", Angulo, " é igual a:",S)

fimalgoritmo